[{"o": "  const t = S.threads.find(x=>x.id===run.threadId);\n  const dur = (run.endedAt||Date.now()) - run.startedAt;\n  const others = S.threads.filter(x=>x.runIds.length && x.id!==run.threadId);", "n": "  const t = S.threads.find(x=>x.id===run.threadId);\n  const dur = (run.endedAt||Date.now()) - run.startedAt;\n  const allRuns = Object.values(S.runs)\n    .filter(r=>r.id!==run.id)\n    .sort((a,b)=>b.startedAt-a.startedAt)\n    .slice(0,20)\n    .map(r=>{ const th=S.threads.find(x=>x.id===r.threadId);\n      return { id:r.id, label:(th?th.title.slice(0,26):'?')+' · '+String(r.remoteRunId||r.id).slice(0,8)+' · '+r.status }; });"}, {"o": "          ${others.length?`<span>·</span><select class=\"input\" id=\"tele-pick\" style=\"width:auto;padding:2px 24px 2px 8px;font-size:11px;font-family:var(--f-mono);background-color:transparent\">\n            <option value=\"\">other runs…</option>\n            ${others.map(o=>`<option value=\"${o.runIds[o.runIds.length-1]}\">${esc(o.title.slice(0,42))}</option>`).join('')}\n          </select>`:''}", "n": "          ${allRuns.length?`<span>·</span><select class=\"input\" id=\"tele-pick\" style=\"width:auto;padding:2px 24px 2px 8px;font-size:11px;font-family:var(--f-mono);background-color:transparent\">\n            <option value=\"\">other runs…</option>\n            ${allRuns.map(o=>`<option value=\"${o.id}\">${esc(o.label)}</option>`).join('')}\n          </select>`:''}"}, {"o": "  $('#strip-tokens').textContent = 'burn ' + fmtTok(S.totalTokens) + ' tok';", "n": "  const liveTot = Object.values(S.runs).filter(r=>r.live).reduce((a,r)=>a+(r.tokens|0),0);\n  $('#strip-tokens').textContent = 'burn ' + fmtTok(S.mode==='live' ? liveTot : S.totalTokens) + ' tok';"}, {"o": "      if (out.length){ t.messages = out; renderChat(); }\n      else pushToastOnce('History','no displayable messages on that gateway thread');", "n": "      if (out.length){\n        t.messages = out;\n        if (/^gateway thread /.test(t.title)){\n          const firstHuman = out.find(m=>m.who==='you');\n          if (firstHuman && firstHuman.body){\n            const nt = String(firstHuman.body).trim().replace(/\\s+/g,' ');\n            t.title = nt.length<64 ? nt : nt.slice(0,61).replace(/\\s+\\S*$/,'')+'…';\n            renderOps();\n          }\n        }\n        renderChat();\n      }\n      else pushToastOnce('History','no displayable messages on that gateway thread');"}, {"o": "    <span>BLIND v0.6</span><span class=\"sep\">|</span>", "n": "    <span>BLIND v0.7</span><span class=\"sep\">|</span>"}]